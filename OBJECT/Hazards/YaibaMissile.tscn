[gd_scene format=3 uid="uid://dkxw3vj0s2u85"]

[ext_resource type="Texture2D" uid="uid://cbkhv1wt60io0" path="res://Sprites/Hazards/Missile.png" id="1"]

[sub_resource type="GDScript" id="2"]
script/source = "extends Area2D

@export var speed = 350
@export var steer_force = 200.0

var velocity = Vector2.ZERO
var acceleration = Vector2.ZERO
var target = null

func start(_transform):
	global_transform = _transform
	rotation += randf_range(-0.09, 0.09)
	velocity = transform.x * speed
	target = Global.player1


func seek():
	var steer = Vector2.ZERO
	if is_instance_valid(target):
		var desired = (target.position - position).normalized() * speed
		steer = (desired - velocity).normalized() * steer_force
	return steer

func _physics_process(delta):
	acceleration += seek()
	velocity += acceleration * delta
	velocity = velocity.limit_length(speed)
	rotation = velocity.angle()
	position += velocity * delta

func die():
	var explosion = preload(\"res://OBJECT/Hazards/MissileExp.tscn\").instantiate()
	explosion.global_position = global_position
	get_parent().add_child(explosion)
	queue_free()
func _on_Lifetime_timeout():
	queue_free()


func _on_Missile_area_entered(area):
	if area.is_in_group(\"Pl_Attack\"):
		
		die()
	
"

[sub_resource type="RectangleShape2D" id="1"]
size = Vector2(7, 30)

[node name="Missile" type="Area2D" unique_id=774573684 groups=["En_Attack", "Enemy"]]
script = SubResource("2")
speed = 50

[node name="Bll" type="Sprite2D" parent="." unique_id=2084819553]
rotation = 1.5708
texture = ExtResource("1")

[node name="CollisionShape2D" type="CollisionShape2D" parent="." unique_id=77321021]
position = Vector2(0, 1)
rotation = 1.56654
shape = SubResource("1")

[node name="Lifetime" type="Timer" parent="." unique_id=1599615015]
wait_time = 4.0
autostart = true

[connection signal="area_entered" from="." to="." method="_on_Missile_area_entered"]
[connection signal="timeout" from="Lifetime" to="." method="_on_Lifetime_timeout"]
